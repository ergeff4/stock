# Cloudflare Pages configuration.
# Cloudflare reads this during the build; `wrangler pages dev` reads it locally.
name = "stock-signal-analyzer"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
